/**
 * Версия приложения, подставляемая Vite на этапе сборки из android/app/build.gradle
 * (см. vite.config.ts). Используется как запасной источник для экрана «О приложении».
 */
declare const __APP_VERSION__: string
